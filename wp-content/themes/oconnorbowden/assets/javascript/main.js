(function($) {
  $(document).ready(function() {
    const getUrlParameter = require('./vendor/getUrlParameter');
    const icons = require('./components/icons');
    const forms = require('./components/forms');
    const sliders = require('./components/sliders');
    const tweens = require('./components/tweens');
    const navigation = require('./components/navigation');
    const partners = require('./components/partners');
    const maps = require('./components/maps');
    const searchResults = require('./components/search_results');
    const property = require('./components/property');
    const gridBlock = require('./components/grid_block');
    const imageGalleryWithContent = require('./components/image-gallery-with-content');
  });
})(jQuery);
